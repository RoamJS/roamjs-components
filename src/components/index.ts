throw new Error(
  `Do not import from roamjs-components/components directly. Instead import the *component* you need directly. For example: 
  
import AutocompleteInput from "roamjs-components/util/AutocompleteInput;"`
);
