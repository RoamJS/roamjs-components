throw new Error(
  `Do not import from roamjs-components/util directly. Instead import the *method* you need directly. For example: 
    
import runExtension from "roamjs-components/util/runExtension;"`
);
